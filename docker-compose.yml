services:
  postgres:
    image: postgres:16-alpine
    container_name: wumpus-db
    environment:
      POSTGRES_DB: wumpus
      POSTGRES_USER: wumpus
      POSTGRES_PASSWORD: wumpus_secret
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata: